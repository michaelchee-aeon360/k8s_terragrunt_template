name: Terragrunt Plan

on:
  pull_request:
    branches: [main]
    paths:
      - "terragrunt/**"

jobs:
  format-validation-plan:
    name: PR Format Checks, Validation, and Plan
    uses: ./.github/workflows/terragrunt-reuse-plan.yaml
