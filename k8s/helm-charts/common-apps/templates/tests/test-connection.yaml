apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "common-apps.fullname" . }}-test-connection"
  labels:
    {{- include "common-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "common-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
