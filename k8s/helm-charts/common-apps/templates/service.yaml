apiVersion: v1
kind: Service
metadata:
  name: {{ include "common-apps.fullname" . }}
  labels:
    {{- include "common-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common-apps.selectorLabels" . | nindent 4 }}
